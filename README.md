## Trello Clone

#### Dependencies
* SQLite

#### Installation steps
* `bundle install`
* `yarn install`
* `rails db:setup`

### Starting the app
* `foreman start` # starts two servers (ports 3000 & 3001)
* visit http://localhost:3001
